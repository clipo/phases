# Hierarchical convergence model: NOT FIT

The panel has undefined cells: [('fst', 0), ('fst', 4)] (signature, bin). A bin whose assemblages all fall in one spatial cluster has no between-cluster F_ST.
This model is demoted and not reported; see `output/findings/perbin_bayesian_fst.md`, which handles the same bin explicitly.
